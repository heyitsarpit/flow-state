// Deferred inspect route: the inspection owner will restore these exports with their proofs.
// analyzeTrace, attachInspectionSink, buildBehaviorContract, compressTraceArtifact,
// createInspectionBufferSink, decompressTraceArtifact, diffBehaviorContracts, diffTrace,
// exportTraceArtifact, formatInspectionEvent, formatInspectionTimeline, formatNoTransitionSummary,
// formatRehydrationSummary, formatResourceFreshnessReport, formatTrace, formatTransactionOverlapSummary,
// graphOf, importTraceArtifact, inspectActivities, inspectMicrosteps, inspectTransition,
// renderBehaviorContract, renderBehaviorCoverage, renderBehaviorDiff, sliceBehaviorContract,
// summarizeTrace, whyNoTransition
export {};
