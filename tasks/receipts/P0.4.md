# P0.4 packet receipt

Packet: P0.4 Child contract reconciliation
Dependencies: None; P0.4 has no dependency receipts.
Base commit: f359d0cc6643d234f9b79e5939fd756709db2624
Base tree: Clean tracked tree before P0.4; ignored generated output was left untouched. `pnpm fmt` later reformatted three unrelated files, and those formatter-only spillovers were restored before staging.
Commit proof: derived-from-git-history
Files: `API_CONTRACT.md`, `TYPE_INFERENCE_CONTRACT.md`, `TASK.md`, `tasks/receipts/P0.4.md`.
Owner after change: `API_CONTRACT.md` and `TYPE_INFERENCE_CONTRACT.md` now own the active child public/type contract. Runtime child behavior remains owned by actor runtime supervision, and no production semantic owner changed.
Defect closed: BUG-17. The active contracts no longer promise child `input`, outcome `routes`, or independent child output/failure propagation that the current public `FlowChildConfig<Machine>` type cannot express.
Effect map: N/A — contract-only packet; no Effect operation, service, or `A/E/R` channel changed.
Layer/lifetime: N/A — no Layer, Scope, fiber, acquisition, finalizer, or runtime lifetime changed.
Native primitives: N/A — no runtime implementation or Effect primitive changed.
Failure lanes: No production failure lane changed. Child typed output/failure route typing is explicitly future additive work rather than an active contract claim.
Reused: Current `flow.child({ id, machine, supervision? })` calls, `FlowChildConfig<Machine>`, `FlowChildDefinition<Machine>`, child runtime supervision, public type tests, and Launch Workspace child usage remain unchanged as compatibility evidence.
Merged/moved: The child contract now routes through the compatibility floor selected by DEC-14: exact child machine plus supervision today; richer child input/routes/output/failure only through a separately approved additive packet.
Removed: Unsupported active contract claims for child input selectors, outcome routes, and independent child output/failure propagation. No source code was deleted.
Rejected clones: No second child constructor, trailing machine generics, bivariant callback workaround, Flow wrapper, Effect helper, retry budget, or parallel child runtime was introduced.
Compatibility: Existing `flow.child({ id, machine, supervision? })` source calls remain accepted. The packet changed prose contracts only and did not remove a public value, import path, alias, call form, runtime output, receipt shape, or wire field.
Tests added: None. P0.4 explicitly has no runtime test; compile/type probes record current expressible child shape.
Commands:

- Baseline `git rev-parse HEAD` — exit `0`; base commit was `f359d0cc6643d234f9b79e5939fd756709db2624`.
- Baseline `git status --short` — exit `0`; clean tracked tree.
- Baseline `pnpm --filter flow-state check:cli-source-types` — exit `0`.
- Supplemental compile probe `pnpm --filter flow-state exec tsc -p tsconfig.json --noEmit --pretty false` — exit `0`.
- Contract/source probe `rg -n "readonly input|readonly routes|output|failure|restart|retry|budget|FlowChildConfig|FlowChildDefinition|flow\\.child\\(" ...` — exit `0`; showed `FlowChildConfig<Machine>` exposes only `id`, `machine`, and optional `supervision`, existing Launch/public child calls use that shape, and the only overclaims were contract prose.
- Final `pnpm --filter flow-state check:cli-source-types` after contract reconciliation — exit `0`.
- Final `pnpm fmt && pnpm lint` — exit `1`; formatting completed across 399 files, then lint reproduced the pre-existing BUG-21 Launch Workspace dependency-resolution cascade (`Cannot find module 'flow-state'`, missing React/Node/@effect-vitest types, and resulting implicit-any diagnostics). Three formatter-only out-of-scope edits were restored before staging.

Review: Thermo-nuclear review found no blocking implementation issue because the packet removed unsupported promises instead of adding a child API clone, widening types, introducing bivariant callbacks, or editing production child runtime/type surfaces. Effect-native review was N/A for code, with the important guard that no `A/E/R`, Scope, Cause, finalizer, retry, or supervision semantics were altered or claimed.
Authority changes: Semantic/type-contract reconciliation. The active child compatibility floor is now exact child machine plus optional supervision; child input selectors, outcome routes, independent output/failure generics, and automatic restart budgets are future additive work requiring approval. No done packet needs revalidation because P0.4 resolves a known pre-implementation conflict and all child implementation packets were already blocked on this receipt.
Still open: P0.5 is next and must inventory semantic owners, duplicate engines, and deletion candidates. P0.1b, P0.3, and P0.6 remain blocked until their declared dependencies close. P3D.1 still owns current child contract and exact typing after P0.6/P1C.5 prerequisites; richer child input/routes/output/failure remains outside active scope until separately approved.
