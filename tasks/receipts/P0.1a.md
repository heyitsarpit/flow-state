# P0.1a packet receipt

Packet: P0.1a Immutable public and behavioral baseline
Dependencies: None; P0.1a has no dependency receipts.
Base commit: ff0f29b8dc03d451a92cecd807a075ca65212c84
Base tree: Clean tracked tree; ignored `packages/flow-state/dist/` existed in an unfingerprinted stale state, and there was no pre-existing tracked user or planning work.
Packet commit: 3dad3cc4b47f4ef79cb0dd4440bb9af5f353268e
Closeout proof: derived-from-git-history
Packet files: `architecture/correctness/BASELINE.md`, `TASK.md`, `tasks/templates/PACKET.md`, `tasks/receipts/README.md`, `tasks/PHASE_0.md`.
Closeout files: `tasks/receipts/P0.1a.md`, `TASK.md`.
Owner after change: N/A — this observational packet changes no semantic owner or public type family; `architecture/correctness/BASELINE.md` owns the durable P0.1a evidence.
Defect closed: N/A — no BUG is closed; P0.1a closes the missing immutable base, environment, generated-state, public-export, caller, and behavioral baseline.
Effect map: N/A — no Effect service or operation changed, so there are no consumed/produced services or A/E/R changes.
Layer/lifetime: N/A — no Layer, acquisition, Scope, fiber, finalizer, or runtime lifetime changed.
Native primitives: N/A — no runtime implementation changed, so no Ref, SynchronizedRef, Deferred, FiberMap, Queue, or equivalent primitive was selected.
Failure lanes: No production failure lane changed. The baseline retains the historical stale-output lane, normalized package-test failures, Launch Workspace dependency-resolution failures, declaration-byte instability, and generated-document drift as explicit evidence.
Reused: Existing package manifests, five public entry sources, package tests, Launch Workspace tests, build/typecheck scripts, runtime artifacts, and caller inventories were retained as the observational sources.
Merged/moved: None; no caller or semantic path moved. Baseline and closeout-process evidence were consolidated into the five Packet files.
Removed: None; this packet is observational and process-only.
Rejected clones: No bespoke Effect, DI, cache, queue, retry, timer, build, or test helper was introduced.
Compatibility: No production source, package manifest, test, public import, runtime value export, or source type inventory changed. Across both declaration variants, all 21 runtime `.mjs` hashes and the five public runtime export sets remained stable: 23 root, 4 React, 10 testing, 1 server, and 34 inspect values.
Tests added: None; P0.1a records existing tests and command evidence without adding an oracle or changing behavior.
Commands:

- Initial historical `pnpm --filter flow-state test` — exit `1`; 79 files and 509 tests, 466 passing and 43 failing in 5 files against unknown stale ignored `dist`. The four additional local-proof failures disappeared after normalization, so this is historical environment evidence only.
- Initial `pnpm --filter flow-state check:cli-source-types` — exit `0`; its exact initial generated delta was not captured.
- Initial `pnpm --filter flow-state build` — exit `0`; cleaned 53 stale entries, emitted 34 bundle files, left 45 ignored files, and passed size/hygiene at raw `213,967` and gzip `41,368` bytes against baselines `223,466` and `43,517` with maximum growth `1.05`.
- Controlled and remediation `pnpm --filter flow-state build` runs — exit `0`; observed approved 45-file declaration variants `744919ad3a3adfd009dd5acb84de1268d29f05b3bcb15c2e3ff581894015b8f0` and `b49b8f6d77ce7bb909a858e4662cf6e39de1835ac49ee6929e3d74825f320e65`. The final closeout verifier emitted the approved `744919...` variant.
- Final `pnpm --filter flow-state check:cli-source-types` — exit `0`; preserved all 45 build hashes and added only `dist/tsconfig.tsbuildinfo`, size `197,142` bytes and SHA-256 `30b1f675c253579281e5d37408d9f9c41feac355a885ec38b95d6f49db5e8ce0`, producing 46-file aggregate `e0f6bb6606de56f8d04d72d024517ba4824c65b42a1eace35ad74f3162f2df1c`.
- Normalized and final `pnpm --filter flow-state test` — exit `1`; exactly 79 files and 509 tests, with 470 passing and 39 failing in the same 5 files. All normalized literal runs used the approved `744919...` declaration variant and preserved the `e0f6bb...` 46-file state.
- Supplemental `pnpm exec vitest run packages/flow-state/src --reporter=json --outputFile=/tmp/p01a-post-build-vitest.json` — exit `1`; repeated 470 passing and 39 failing tests and captured the exact normalized failure IDs without replacing the literal package command.
- `pnpm --filter @flow-state/launch-workspace test -- --run` — exit `1`; 10 suites failed at import, 3 passed, and 10 tests passed because package-local dependencies are absent.
- `pnpm --filter @flow-state/launch-workspace check:typescript-mode-proofs` — exit `2`; missing React, Next, `flow-state`, `@effect/vitest`, and Node types produced the approved cascading diagnostics.
- `pnpm --filter @flow-state/launch-workspace build` — exit `2`; the same TypeScript proof failed before `next build` ran.
- Final Packet `pnpm fmt && pnpm lint` — exit `1`; formatting completed across 396 files, then lint reproduced the approved Launch Workspace dependency-resolution cascade. The three provably formatter-only out-of-scope edits were restored.
- Packet `pnpm exec vp fmt --check architecture/correctness/BASELINE.md TASK.md tasks/templates/PACKET.md tasks/receipts/README.md tasks/PHASE_0.md` — exit `0`; all five Packet files were formatted.
- Packet worktree allowlist comparison, `git diff --check`, cached allowlist comparison, and `git diff --cached --check` — exit `0`; the Packet commit contained exactly its five authorized files.
- Closeout `pnpm vp fmt --check TASK.md tasks/receipts/P0.1a.md`, two-file cached allowlist comparison, and `git diff --cached --check` — exit `0` before the metadata-only Closeout commit.

Review: Thermo review round one approved the 39 normalized failure IDs, four initial-only IDs, both controlled aggregate hashes, tsbuildinfo evidence, all five value/type export sets, caller inventory, scope, and file health. The first final verifier stopped on the `b49b8f...` declaration variant; the baseline was corrected to distinguish declaration-byte instability from stable runtime/public invariants. Thermo review round two approved that correction, independently verified both variants, stable runtime hashes and exports, and the limitation that package-test evidence covers only `744919...`. Contract review identified that one immutable commit could not contain its own SHA; the user authorized the two-commit protocol, the process authorities were updated without weakening the exact-SHA requirement, and a fresh contract reviewer approved the exact five-file Packet diff. Final verification and Packet staging found no new blocking issue.
Authority changes: Process-only. `TASK.md`, `tasks/templates/PACKET.md`, `tasks/receipts/README.md`, and `tasks/PHASE_0.md` now require a reviewed Packet commit followed immediately by a metadata-only Closeout commit whose receipt names Packet and whose existence is derived from Git history. No semantic or behavioral acceptance criterion, dependency, public contract, or packet scope changed, so no downstream packet needs revalidation.
Still open: P0.2 owns Launch Workspace executable-truth reconciliation and is next. P0.4 owns child-contract reconciliation. P0.5 owns semantic-owner and duplicate-engine inventory. P0.1b owns BUG-21 tooling/build-resolution repair after P0.2, P0.4, and P0.5 close. Declaration-build byte instability has no proved cause and remains recorded rather than assigned or fixed here; generated API-reference drift and the two missing behavior-document sources remain with their owning later packets.
