# P0.1b packet receipt

Packet: P0.1b Tooling and build-resolution baseline
Dependencies: P0.1a, P0.2, P0.4, and P0.5.
Base commit: 78d8ca7af7cfa8e683c1d37cae6958a4adaf08e2
Base tree: Clean tracked tree before P0.1b; local `examples/launch-workspace/node_modules` was missing and was restored from the lockfile during diagnosis.
Commit proof: derived-from-git-history
Files: `TASK.md`, `package.json`, `packages/flow-state/package.json`, `pnpm-lock.yaml`, `scripts/check-lint-build-resolution.mjs`, `tasks/receipts/P0.1b.md`, plus formatter-only gate cleanup in `packages/flow-state/scripts/check-build-output.mjs`.
Owner after change: Root `package.json` owns the root lint preflight: `pnpm lint` now runs `pnpm lint:prepare && vp lint`, and `lint:prepare` runs the `flow-state` package build that produces the declarations Launch Workspace consumes through package exports.
Defect closed: BUG-21. Root lint no longer reports missing `flow-state`, React, Next, Node, or `@effect/vitest` cascades from an unprepared installed checkout; it prepares `flow-state` package output first and then reports real source diagnostics.
Effect map: N/A — no Effect service, operation, `A/E/R` channel, or runtime orchestration path changed.
Layer/lifetime: N/A — no Layer, Scope, fiber, acquisition, finalizer, or runtime lifetime changed.
Native primitives: N/A — no runtime primitive changed.
Failure lanes: Build/lint failure lanes changed only in tooling: stale or broken `flow-state` source now fails during `lint:prepare`, missing declared build tools fail at package build time, and Launch Workspace source diagnostics remain visible after declaration preparation.
Reused: Existing package exports, `flow-state` build, `prepare-build-output`, `check-build-output`, `vp lint`, Launch Workspace workspace dependency, and type-aware linting remain the source of truth.
Merged/moved: Root lint now composes the existing package build before the existing type-aware lint command. No runtime source moved.
Removed: No runtime code, public export, receipt shape, wire format, or compatibility alias was removed.
Rejected clones: No source-path alias, alternate `flow-state` shim, stale-dist fallback, deep import, generated declaration stub, second lint engine, or weakened type-aware lint mode was introduced.
Compatibility: Public import paths still resolve through package exports and `dist` declarations. The new preflight makes that installed-checkout behavior reproducible instead of relying on stale output or accidental local symlinks.
Tests added: `scripts/check-lint-build-resolution.mjs` verifies that root lint prepares declarations first, that the `flow-state` package build still owns declaration preparation, that the package declares the `esbuild` binary used by `prepare-build-output`, that Launch Workspace depends on `flow-state` through `workspace:*`, and that exported type conditions resolve through `dist`.
Commands:

- Baseline `git rev-parse HEAD` — exit `0`; base commit was `78d8ca7af7cfa8e683c1d37cae6958a4adaf08e2`.
- Baseline `git status --short` — exit `0`; clean tracked tree.
- BUG-21 reproduction `pnpm lint 2>&1 | sed -n '1,160p'` — exit `0` because the pipe ended in `sed`; output reproduced the missing-module cascade for `flow-state`, `flow-state/server`, `flow-state/inspect`, `flow-state/testing`, React JSX types, and resulting implicit-`any` diagnostics before the fix was applied.
- Supplemental installed-checkout proof `pnpm --filter @flow-state/launch-workspace check:typescript-mode-proofs` before restoring install state — exit `2`; output reproduced the same missing package/type cascade and showed `examples/launch-workspace/node_modules` was absent.
- `CI=true pnpm install --offline --frozen-lockfile --ignore-scripts` — exit `1`; the pnpm store lacked `@types/node` while recreating `node_modules`.
- Escalated `CI=true pnpm install --frozen-lockfile --ignore-scripts` — exit `0`; restored the workspace install from the lockfile without tracked changes.
- After adding `esbuild` as a direct `flow-state` dev dependency, `CI=true pnpm install --offline --ignore-scripts` — exit `1`; CI defaulted to a frozen lockfile and refused the changed importer.
- `CI=true pnpm install --offline --ignore-scripts --no-frozen-lockfile` — exit `1`; the pnpm store lacked `typescript`.
- Escalated `CI=true pnpm install --ignore-scripts --no-frozen-lockfile` — exit `0`; updated the `pnpm-lock.yaml` importer entry and linked `packages/flow-state/node_modules/.bin/esbuild`.
- Focused tooling contract `node scripts/check-lint-build-resolution.mjs` — exit `0`.
- Stale-dist negative probe: temporary `packages/flow-state/src/p0-1b-stale-dist-probe.ts`, then `pnpm lint` — exit `2`; `lint:prepare` failed on `TS2322` before `vp lint`, proving stale `dist` cannot satisfy the root lint gate.
- Real-diagnostic negative probe: temporary `examples/launch-workspace/src/p0-1b-real-diagnostic-probe.ts`, then `pnpm lint` — exit `1`; `lint:prepare` completed, then `vp lint` reported `TS2305` and `TS2322` from the temporary Launch source file with no missing-package cascade.
- Final `node scripts/check-lint-build-resolution.mjs` — exit `0`.
- Final `pnpm --filter flow-state check:cli-source-types` — exit `0`.
- Final `pnpm --filter flow-state build` — exit `0`; package declarations, CLI output, bundle-size baseline, and build-output hygiene all passed.
- Final `pnpm lint` — exit `0`; root lint prepared `flow-state` output and completed with existing warnings only.
- Final `pnpm fmt && pnpm lint` — exit `0`; lint completed after declaration preparation. Formatter-only cleanup was retained only for the package build-output checker; Launch Workspace script formatter hunks were later reverted as out-of-scope because staged-file pre-commit type checking reports pre-existing Node-type issues for that script.

Review: Thermo-nuclear review found the fix narrow enough for P0.1b: it prepares the real package output that installed consumers use instead of adding source aliases or bypassing package exports, the direct `esbuild` dependency makes an existing package-local build command honest, stale output is rejected by the preflight, and real Launch Workspace diagnostics still surface after the preflight succeeds.
Authority changes: Root lint is now a build-resolution gate, not a raw `vp lint` invocation over whatever package output happens to be present. P0.3 and P0.1c are now ready; P0.3 is the next packet because it was already the next independent semantic proof packet.
Still open: P0.3 must add compact semantic type sentinels. P0.1c must add packed/performance fixtures and measurements before P0.6 can synthesize Phase 0.
