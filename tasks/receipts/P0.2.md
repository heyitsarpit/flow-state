# P0.2 packet receipt

Packet: P0.2 Launch Workspace executable-truth reconciliation
Dependencies: None; P0.2 has no dependency receipts.
Base commit: e5c746a649e62c9e82591db8d6081e2a0d63cfec
Base tree: Clean tracked tree before P0.2; all ten files in the packet commit are packet output.
Commit proof: derived-from-git-history
Files: `README.md`, `TASK.md`, `apps/docs/src/pages/guide/launch-workspace.md`, `apps/docs/src/pages/reference/api.mdx`, `apps/docs/src/pages/reference/status.mdx`, `examples/launch-workspace/API_INVENTORY.md`, `examples/launch-workspace/PHASE_0_TEST_CHECKLIST.md`, `examples/launch-workspace/README.md`, `packages/flow-state/src/status-docs-architecture.test.ts`, `tasks/receipts/P0.2.md`.
Owner after change: `examples/launch-workspace/API_INVENTORY.md` owns Launch-specific evidence classification; `API_CONTRACT.md` remains the governing public contract, and no production semantic owner changed.
Defect closed: BUG-13 and BUG-15. Launch documentation and inventory now agree on evidence-derived status without recreating the missing `reference-next/lib-api.md` authority.
Effect map: N/A — documentation and structural architecture tests changed; no Effect operation, service, or A/E/R channel changed.
Layer/lifetime: N/A — no Layer, Scope, fiber, acquisition, finalizer, or runtime lifetime changed.
Native primitives: N/A — no runtime implementation or primitive changed.
Failure lanes: No production lane changed. The matrix preserves unproved runtime behavior as partial or contract-only and keeps BUG-14/39 assigned to P4A.3.
Reused: Existing declarations, semantic owners, runtime executors, Launch tests, `API_CONTRACT.md`, and package status documentation remain the evidence sources.
Merged/moved: Launch-specific API truth moved into one five-fact matrix; supporting README, guide, reference, checklist, and architecture-test claims now route to it.
Removed: The dead `reference-next/lib-api.md` pointer was removed from the active inventory; no implementation was deleted.
Rejected clones: No parallel API authority, runtime oracle, semantic engine, Effect helper, cache, queue, retry, or timer implementation was introduced.
Compatibility: No production source, public call, import, alias, runtime output, receipt shape, or wire format changed. `flow.run`, `flow.patch`, and `flow.after` remain contract-only in Launch proof; `flow.observe` remains partial.
Tests added: Strengthened `status docs architecture` coverage for the complete path-valid Launch matrix, resource-command classification, P4A.3 receipt-history boundary, and coarse status-registry boundary.
Commands:

- Focused red proof after strengthening the structural contract — exit `1`; two failures exposed the former four-column inventory and stale classifications.
- Final `pnpm exec vitest run packages/flow-state/src/status-docs-architecture.test.ts packages/flow-state/src/docs-information-architecture.test.ts examples/launch-workspace/src/launchWorkspacePackageHygiene.test.ts` — exit `0`; 3 files and 11 tests passed.
- `pnpm docs:build` — exit `1` before Vocs; the pre-existing Testing API metadata drift reports unexpected `runFlowStoryWithDiagnostics` and remains outside P0.2.
- Scoped formatting/lint and `git diff --check` during implementation — exit `0` for the eight implementation files.
- Final `pnpm fmt && pnpm lint` — exit `1`; formatting completed across 398 files, then lint reproduced the pre-existing BUG-21 Launch Workspace dependency-resolution cascade. Three formatter-only out-of-scope edits were restored.
- Final `pnpm vp fmt --check README.md TASK.md apps/docs/src/pages/guide/launch-workspace.md apps/docs/src/pages/reference/api.mdx apps/docs/src/pages/reference/status.mdx examples/launch-workspace/API_INVENTORY.md examples/launch-workspace/PHASE_0_TEST_CHECKLIST.md examples/launch-workspace/README.md packages/flow-state/src/status-docs-architecture.test.ts tasks/receipts/P0.2.md` — exit `0`; all ten packet files were formatted.
- Final worktree/cached allowlists, `git diff --check`, and `git diff --cached --check` — exit `0`; the staged packet contained exactly the ten files named above.

Review: The first thermo review found four blockers: test paths were conflated with runtime executors, `flow.run`/`flow.patch`/`flow.observe` were overclaimed, BUG-39 wording implied that P4A.3 bounded current receipt history, and the guide promoted coarse status metadata as runtime proof. All four were fixed. The root agent then directly reviewed the complete slice against the Effect-native and Effect v4 API review references, confirmed the actual package owner/executor paths and Launch assertions, and found no remaining blocker. The architecture test is explicitly structural documentation proof, not an independent runtime oracle.
Authority changes: Documentation-only. The Launch inventory is now the evidence authority for this proof app while `API_CONTRACT.md` remains the governing public contract. No semantic or behavioral acceptance criterion changed, so no downstream packet needs revalidation.
Still open: BUG-14 and BUG-39 remain assigned to P4A.3, where canonical business read models must become independent of receipt retention or truncation. Offline queue/replay, generated hooks, broader cache policy, `flow.observe` lifetime, and standalone `flow.run`/`flow.patch`/`flow.after` execution remain explicitly unproved or deferred. P0.4 is next; P0.5 remains ready. BUG-21 owns the root lint dependency-resolution failure, and generated Testing API metadata drift remains outside P0.2.
