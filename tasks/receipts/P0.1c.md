# P0.1c packet receipt

Packet: P0.1c Packed and performance fixtures
Dependencies: P0.1b.
Base commit: a5079dc6215cf84a3df6fa2fe8d3ceb45a79225c
Base tree: Clean tracked tree before P0.1c; ignored generated output was left untouched except for proof/build commands that regenerate ignored `dist` directories.
Commit proof: derived-from-git-history
Files: `TASK.md`, `architecture/correctness/BASELINE.md`, `examples/typescript-proof-packed-react-18/**`, `examples/typescript-proof-packed-react-19/**`, `packages/flow-state/package.json`, `packages/flow-state/scripts/check-typescript-mode-proofs.mjs`, `packages/flow-state/scripts/measure-p0-1c-baseline.mjs`, `packages/flow-state/src/package-hygiene.test.ts`, `pnpm-lock.yaml`, `tasks/receipts/P0.1c.md`.
Owner after change: `architecture/correctness/BASELINE.md` owns the P0.1c packed-consumer and measurement baseline, while `packages/flow-state/scripts/measure-p0-1c-baseline.mjs` owns the repeatable measurement command that P5.4 can rerun.
Defect closed: N/A — P0.1c is a baseline fixture packet. It does not close BUG-38, BUG-18T, BUG-18M, BUG-18S, or any runtime defect.
Effect map: Type/build/performance fixture only. No Effect service, operation, `A/E/R` channel, or runtime orchestration path changed. The measurement script uses existing public runtime APIs to measure current behavior.
Layer/lifetime: No Layer, Scope, fiber, acquisition, finalizer, or runtime lifetime changed. The script creates and disposes existing public runtimes during measurements.
Native primitives: Baseline only. The script measures current key/ref creation, resource seeding/subscription/patching, actor mailbox flush, transaction pressure, stream pressure, receipt retention, and actor restore without replacing any primitive.
Failure lanes: Packed type failures now fail `check:typescript-mode-proofs` for root, React 18, React 19, testing, server, inspect, and private/deep-import negatives. The React 18 fixture maps `react` type imports to its own type package so the proof models peer-owned consumer types rather than the workspace symlink's React 19 dev types.
Reused: Existing typecheck proof package pattern, package exports, build-output gate, package hygiene tests, Launch Workspace declaration proof, public runtime API, and P0.1a `BASELINE.md`.
Merged/moved: The existing TypeScript mode proof runner now includes packed React 18 and packed React 19 packages. No runtime source moved.
Removed: None. No production API, export, compatibility alias, runtime behavior, receipt shape, or wire format was removed.
Rejected clones: No generated benchmark DSL, alternate type runner, source-path package alias, runtime shim, fake packed declaration, latency budget, or production perf gate was introduced.
Compatibility: Public imports still resolve through package exports. The new packed fixtures prove root/React/testing/server/inspect imports and intentionally reject private/deep import paths through the existing multi-entry negative checks.
Tests and fixtures added:

- `examples/typescript-proof-packed-react-18` compiles the React entry under React 18 consumer types.
- `examples/typescript-proof-packed-react-19` compiles the React entry under React 19 consumer types.
- `packages/flow-state/scripts/measure-p0-1c-baseline.mjs` records TypeScript extended diagnostics, declaration bytes, package payload bytes, Launch Workspace declaration behavior, and small/medium/adversarial scaling tiers.
- `package-hygiene.test.ts` now requires the packed React proof packages and the P0.1c measurement script entry.

Measurement baseline: `architecture/correctness/BASELINE.md` records the final P0.1c run from `pnpm --filter flow-state p0-1c:measure`: multi-entry instantiations `59,814`, React 18 instantiations `13,881`, React 19 instantiations `14,453`, `dist` declaration bytes `152,351`, publish payload proxy bytes `2,144,809`, Launch Workspace declaration proof exit `0`, and small/medium/adversarial tiers of `64`, `256`, and `1,024` operations with key depths `3`, `6`, and `12`.
Commands:

- Baseline `git rev-parse HEAD` — exit `0`; base commit was `a5079dc6215cf84a3df6fa2fe8d3ceb45a79225c`.
- Baseline `git status --short` — exit `0`; clean tracked tree.
- Baseline Launch Workspace declaration proof `pnpm --filter @flow-state/launch-workspace check:typescript-mode-proofs` — exit `0`.
- Dependency lookup `pnpm view react@18 version && pnpm view @types/react@18 version && pnpm view @types/react-dom@18 version` — exit `0`; used to select existing React 18 fixture versions.
- `pnpm install --ignore-scripts --no-frozen-lockfile` — exit `0`; updated workspace lockfile for the new React 18 and React 19 proof packages.
- First `pnpm --filter flow-state check:typescript-mode-proofs` after adding React fixtures — exit `1`; React 18 `createElement(FlowProvider, ...)` saw the workspace symlink's React 19 dev types. The React 18 tsconfig was narrowed to resolve non-relative `react` imports through its own fixture type package.
- Final `pnpm --filter flow-state check:typescript-mode-proofs` after that fix — exit `0`.
- First `pnpm --filter flow-state p0-1c:measure` — exit `0`; proved the measurement script, then a comparator lint warning was fixed.
- `node --check packages/flow-state/scripts/measure-p0-1c-baseline.mjs` — exit `0`.
- Focused `pnpm exec vitest run packages/flow-state/src/package-hygiene.test.ts` — exit `0`; 1 file and 10 tests passed.
- Parallel verifier batch `pnpm --filter flow-state build`, `pnpm --filter @flow-state/launch-workspace check:typescript-mode-proofs`, and `pnpm --filter flow-state check:typescript-mode-proofs` — build and Launch proof exited `0`; type proof exited `1` because it raced the build command while `dist` was being cleaned.
- Sequenced rerun `pnpm --filter flow-state check:typescript-mode-proofs` after build completed — exit `0`.
- First `pnpm check` — exit `1`; formatting issues were found in the new baseline/script plus the two known Launch Workspace script formatter-drift files.
- `pnpm vp check --fix architecture/correctness/BASELINE.md packages/flow-state/scripts/measure-p0-1c-baseline.mjs` — exit `0`; formatted only the P0.1c files.
- Second `pnpm check` — exit `1`; only the known Launch Workspace formatter-drift files remained.
- First final `pnpm fmt && pnpm lint` after the receipt/status update — exit `1`; lint rejected `examples/typescript-proof-packed-react-18/tsconfig.json` because `baseUrl` is removed under the current TypeScript/tsgolint rules. The `paths` mapping worked without `baseUrl`, so `baseUrl` was removed.
- `pnpm --filter flow-state check:typescript-mode-proofs` after removing `baseUrl` — exit `0`.
- Second final `pnpm fmt && pnpm lint` — exit `0`; lint completed after declaration preparation with existing warnings only.
- Final `pnpm --filter flow-state p0-1c:measure` after removing `baseUrl` — exit `0`; this is the run recorded in `BASELINE.md`. Launch Workspace script formatter hunks produced by `pnpm fmt` were reverted as out-of-scope before staging.

Review: Thermo-nuclear review found the packet scoped correctly to fixtures and baseline measurement. The React 18 proof models consumer-owned peer types, packed proof remains routed through package exports, the performance data are measurements rather than budgets, and the scaling tiers record operation counts plus allocation proxies without turning GC-sensitive heap deltas into a gate.
Authority changes: P0.1c is now done. P0.6 is the next ready packet because P0.1b, P0.1c, P0.2, P0.3, P0.4, and P0.5 now have receipts.
Still open: P0.6 must synthesize Phase 0 decisions, capacity policy, compatibility corpus, and laws. The known Launch Workspace formatter drift remains outside this packet, and P0.6/P5.4 must decide whether to fold or isolate that gate behavior.
