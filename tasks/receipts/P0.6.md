# P0.6 packet receipt

Packet: P0.6 Semantic decisions, capacity policy, and compatibility corpus
Dependencies: P0.1b, P0.1c, P0.2, P0.3, P0.4, and P0.5.
Base commit: 6e84aed279fa0edea0104cbc16f1da8598c754fc
Base tree: Clean tracked tree before P0.6; ignored generated output was left untouched except for proof/build commands that regenerate ignored `dist` directories.
Commit proof: derived-from-git-history
Files: `TASK.md`, `tasks/SEMANTIC_DECISIONS.md`, `tasks/EFFECT_ARCHITECTURE.md`, `CAPACITY_POLICY.md`, `COMPATIBILITY_CORPUS.md`, `LAWS_AND_ORACLES.md`, `packages/flow-state/src/correctness-plan-architecture.test.ts`, `tasks/receipts/P0.6.md`.
Owner after change: Phase 0 synthesis artifacts now own the semantic handoff into Phase 1. `SEMANTIC_DECISIONS.md` owns DEC-1 through DEC-22 implementation handoff, `EFFECT_ARCHITECTURE.md` owns the concrete service/Layer/Scope graph, `CAPACITY_POLICY.md` owns default/max capacity decisions, `COMPATIBILITY_CORPUS.md` owns compatibility surfaces, and `LAWS_AND_ORACLES.md` owns law/non-law proof rules.
Defect closed: N/A — P0.6 is a synthesis packet. It unblocks Phase 1 but does not implement production behavior or close a runtime defect.
Effect map: Documentation/test only. The Effect blueprint now names ResourceStore, OrchestratorSystem, transactions, streams, timers, children, evidence, hydration, React, server, testing, and CLI service/layer/scope ownership with the required primitive commitments; no Effect code changed.
Layer/lifetime: Documentation/test only. The service graph records host config/services into client Layers, RuntimeCoreLayer, scoped runtime services, actor scopes, and ManagedRuntime host boundaries.
Native primitives: Documentation/test only. Capacity and Effect architecture record `Ref`, `SynchronizedRef`, `FiberMap`, `FiberSet`, bounded `Queue`, bounded `PubSub`, `Semaphore`, `Stream`, `Clock`/`TestClock`, `Deferred`, `Schema`, `Exit`, and `Cause` commitments for later packets.
Failure lanes: The semantic handoff records domain failure, decode rejection, unsupported input, conflict, stale, defect, interruption, cleanup, observer, and invariant lanes as discriminated facts behind a compatible public supertype.
Reused: P0.1c measurement command and BASELINE, P0.3 source/packed type fixtures, OWNER_MAP, existing decision register, existing Effect blueprint, and package build/type gates.
Merged/moved: None. P0.6 adds missing synthesis artifacts and architecture assertions without moving runtime code.
Removed: None. No production source, public API, runtime behavior, export, receipt shape, or wire format was removed.
Rejected clones: No generated planning DSL, production helper, runtime shim, benchmark gate, second interpreter, source-text-only proof, or alternate architecture authority was introduced.
Compatibility: Phase 0 compatibility is now split by source API, runtime behavior, receipts/evidence, in-memory snapshots, wire/boot, packed exports, peer/environment, and duplicate package/Effect surfaces. Boot v1 remains the default corpus, and v2 requires separate approval.
Tests added: `packages/flow-state/src/correctness-plan-architecture.test.ts` asserts that P0.6 artifacts exist and retain required decision, Effect, capacity, compatibility, law, P0.1c baseline, and owner-map anchors.
Commands:

- Baseline `git rev-parse HEAD` — exit `0`; base commit was `6e84aed279fa0edea0104cbc16f1da8598c754fc`.
- Baseline `git status --short` — exit `0`; clean tracked tree.
- Focused P0.6 `pnpm exec vitest run packages/flow-state/src/correctness-plan-architecture.test.ts` — exit `0`; 1 file and 5 tests passed.
- P0.3 source fixtures `pnpm exec vitest run packages/flow-state/src/public-api-types.test.ts packages/flow-state/src/public-typing-architecture.test.ts examples/launch-workspace/src/launchWorkspaceTypingArchitecture.test.ts` — exit `0`; 3 files and 62 tests passed.
- P0.1c packed fixtures `pnpm --filter flow-state check:typescript-mode-proofs` — exit `0`; TypeScript mode proofs ok.
- First `pnpm --filter flow-state check:cli-source-types` — exit `2`; the new architecture test had an unused `readFileSync` import. The import was removed.
- Final `pnpm --filter flow-state check:cli-source-types` — exit `0`.
- `pnpm --filter flow-state build` — exit `0`; package declarations, CLI output, bundle-size baseline, and build-output hygiene passed.
- P0.1c measurement input `pnpm --filter flow-state p0-1c:measure` — exit `0`; reran the capacity-measurement input used by `CAPACITY_POLICY.md`.
- Final `pnpm fmt && pnpm lint` — exit `0`; lint completed after declaration preparation with existing warnings only. Launch Workspace script formatter hunks produced by `pnpm fmt` were reverted as out-of-scope before staging.

Review: Thermo-nuclear review found P0.6 scoped to planning artifacts and architecture tests. The artifacts name owners and publication points instead of leaving worker-selectable semantics, capacities are measured inputs rather than hidden constants, compatibility is split by surface, Effect ownership is concrete enough to block parallel DI or `Effect.run*` islands, and laws require independent oracles rather than production helpers.
Authority changes: P0.6 is now done and Phase 0 is closed. P1A.0 is the next ready packet; P1D.1a is also ready but follows P1A.0 in the critical path.
Still open: P1A.0 must implement safe definitions and app identity. P1D.1a must establish the host boundary and runtime Layer contracts. The known Launch Workspace formatter drift remains outside P0.6.
