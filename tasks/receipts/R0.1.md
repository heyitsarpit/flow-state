# R0.1 recovery receipt

Packet: R0.1 Recovery bootstrap and immutable failure ledger
Dependencies: P0.1a baseline and the current P1C.3b tree.
Base commit: bd1a059090698de0898d1b8bf9a1547d4e97c3f8
Base tree: Tracked tree clean; `flow-state-0.0.0.tgz` was an untracked artifact generated by the audited session.
Commit proof: derived-from-git-history
Files: `TASK.md`, `tasks/REVALIDATION.md`, `tasks/receipts/R0.1.md`, `packages/flow-state/src/correctness-plan-architecture.test.ts`, and formatter/staged-check repair in `examples/launch-workspace/scripts/collect-function-outputs.ts`.
Owner after change: `TASK.md` and `tasks/REVALIDATION.md` own the temporary fail-closed recovery queue and one-packet-per-session execution contract.
Defect closed: The forward plan can no longer treat the affected actor sequence as complete or continue indefinitely through newly ready packets. The transient P0.6 test no longer asserts one historical `Ready — next` status.
Effect map: N/A; this packet changes planning, proof routing, and formatter state only.
Layer/lifetime: N/A; no runtime Layer, Scope, fiber, or finalizer changed.
Native primitives: N/A.
Failure lanes: Current failures outside the surviving P0.1a architecture IDs are blocking and routed to R0.2 through R0.10. No failure is waived as merely known or deferred.
Reused: The immutable P0.1a baseline, existing packet manifests, packet receipt contract, and current focused tests remain the evidence sources.
Merged/moved: Forward execution is temporarily routed through one recovery queue; the original phase DAG remains intact behind it.
Removed: The generated `flow-state-0.0.0.tgz` artifact was removed. The recurring formatter disagreement in `collect-function-outputs.ts` was accepted instead of being reformatted and reverted in every packet; its explicit Node type reference keeps isolated staged-file checks valid.
Rejected clones: No second semantic plan, generated task DSL, runtime workaround, or replacement receipt history was introduced.
Compatibility: No public source API, runtime behavior, package export, wire shape, or existing immutable receipt changed.
Tests added: No new test file. `correctness-plan-architecture.test.ts` now checks durable P0.6 artifact wiring instead of transient next-packet prose and proves that recovery sessions stop after one ready packet.
Commands:

- Focused `pnpm exec vitest run packages/flow-state/src/correctness-plan-architecture.test.ts` — exit `0`; 1 file and 6 tests passed.
- T `pnpm --filter flow-state check:cli-source-types` — exit `0`.
- Recovery ledger `pnpm --filter flow-state test` — exit `1`; 13 files failed and 67 passed, with 46 failures and 513 passes. Three failures in `api-reference-generation-architecture.test.ts`, `behavior-guidance-architecture.test.ts`, and `behavior-scaffold-architecture.test.ts` remain from the P0.1a baseline. The other 43 failures span 10 files and are routed to R0.2 through R0.7.
- E `pnpm --filter @flow-state/launch-workspace test -- --run` — exit `1`; 2 files failed and 11 passed, with 8 unregistered-machine failures and 53 passes. R0.8 owns this exact failure set.
- C `pnpm fmt && pnpm lint` — exit `0`; formatting, build, source types, packed output, and lint completed with four existing warnings and no errors.
- Supplemental `pnpm check` — exit `0`; 436 files were formatted, with the same four warnings and no errors.

Review: The thermo-nuclear review found no runtime or Effect change to assess. It replaced a tracker test coupled to transient packet status with durable P0.6 artifact links, froze five contradicted completion claims, split every new broad-suite failure family into a named recovery owner, removed the generated tarball, and kept the tooling repair local to formatting plus the Node ambient type required by the staged-file gate. Focused proof, T, C, the isolated five-file check, and `pnpm check` were green; the two nonzero observational ledgers are preserved as explicit recovery work rather than waived.
Authority changes: P1A.0 and P1C.1 through P1C.3b move to needs-revalidation. Recovery packets may restore them only with current affected evidence. Each `/goal` completes one recovery packet and stops.
Still open: R0.2 is next. R0.3 through R0.10 remain blocked in strict order, and no forward Phase 1 packet may execute until R0.10 closes.
