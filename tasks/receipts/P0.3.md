# P0.3 packet receipt

Packet: P0.3 Compact semantic inference baseline
Dependencies: P0.1a, P0.2, P0.4, and P0.5. P0.1b was already closed before this packet, so the root lint/build-resolution gate was available, but it is not a formal P0.3 dependency.
Base commit: 90848f141a537d668b56ef0717cf4e36609de4f3
Base tree: Clean tracked tree before P0.3; ignored generated output was left untouched.
Commit proof: derived-from-git-history
Files: `TASK.md`, `packages/flow-state/src/public-api-types.test.ts`, `packages/flow-state/typecheck/multi-entry-declarations.ts`, `examples/launch-workspace/src/launchWorkspaceTypingArchitecture.test.ts`, `tasks/receipts/P0.3.md`.
Owner after change: The public type suites now own the compact Phase 0 semantic sentinels for source and packed entry points. They prove representative input-first resource, transaction, stream, machine, Layer, module/app, and packed import behavior without becoming the final type matrix.
Defect closed: BUG-16. Launch Workspace app typing is now checked semantically, so source-text-only confidence can no longer pass while the exported app type silently widens.
BUG-38 status: Still open under P1A.0. P0.3 intentionally proves that the broad `FlowAppDefinition` annotation remains present and erases the exact Launch Workspace app tuple/map; P1A.0 owns removing that annotation after the exact inferred app identity regression is red.
Effect map: Type-test only. No Effect service, operation, `A/E/R` channel, or runtime orchestration path changed. The new resource, stream, transaction, and Layer fixtures assert existing public Effect/Stream/Layer types.
Layer/lifetime: Type-test only. The Layer sentinel proves remaining `ProjectConfig` requirements stay visible and rejects using a still-requiring app layer as a request runtime; no Scope, fiber, acquisition, finalizer, or runtime lifetime changed.
Native primitives: N/A — no runtime primitive changed.
Failure lanes: Source fixtures preserve typed `"missing"` failure for resource/stream examples and prove the Layer requirement lane is not erased. No runtime failure semantics changed.
Reused: Existing public builders, app/module descriptors, Effect `Layer` helpers, Launch Workspace typing architecture tests, package export routes, and the packed declaration typecheck fixture remain the source of truth.
Merged/moved: None. P0.3 adds sentinels to existing focused type-test locations instead of introducing a new type DSL or broad matrix generator.
Removed: None. No production declaration, public export, compatibility alias, runtime behavior, receipt shape, or wire format was removed.
Rejected clones: No alternate constructor syntax, `bind(App)` API, source-path package alias, generated declaration stub, type assertion workaround, second type harness, or source-text-only oracle was introduced.
Compatibility: Current calls and packed imports remain valid. The new private deep-import negative proves `flow-state/core/api/types` is not a packed public entry point.
Tests added:

- `public-api-types.test.ts` now contains one compact source sentinel covering resource params, transaction commit params, stream subscribe params, machine context/state targets, module/app tuple stability, and app Layer requirements.
- `launchWorkspaceTypingArchitecture.test.ts` now asserts the exported Launch Workspace app type is currently the broad `FlowAppDefinition` and keeps source-text architecture checks only as secondary lint.
- `typecheck/multi-entry-declarations.ts` now asserts representative packed imports for root, React, server, testing, and inspect entry points, plus the intentional private deep-import failure.

TypeScript limits recorded: Current `flow.resource({...})` does not infer a typed error/requirement lane from `lookup` without explicit generics, so the P0.3 resource sentinel uses the existing explicit-generic compatibility form rather than inventing syntax. Current transaction, machine, and stream callback-family helpers still use bivariant callback typing; P0.3 proves exact wrong-param/state/Layer sentinels, while unsafe-narrower callback rejection remains owned by BUG-18T in P2.4, BUG-18M in P3A.2, and BUG-18S in P3B.3.
Commands:

- Baseline `git rev-parse HEAD` — exit `0`; base commit was `90848f141a537d668b56ef0717cf4e36609de4f3`.
- Baseline `git status --short` — exit `0`; clean tracked tree.
- Focused `pnpm exec vitest run packages/flow-state/src/public-api-types.test.ts packages/flow-state/src/public-typing-architecture.test.ts examples/launch-workspace/src/launchWorkspaceTypingArchitecture.test.ts` — exit `0`; 3 files and 62 tests passed.
- First `pnpm --filter flow-state check:cli-source-types` — exit `2`; the resource sentinel initially expected lookup error/requirement inference without explicit generics, and a machine negative had the `@ts-expect-error` on the wrong line. The resource fixture was changed to the explicit-generic compatibility form and the machine negative was narrowed to the actual diagnostic site.
- Final `pnpm --filter flow-state check:cli-source-types` — exit `0`.
- Final `pnpm --filter flow-state check:typescript-mode-proofs` — exit `0`; TypeScript mode proofs ok.
- Final `pnpm --filter flow-state build` — exit `0`; package declarations, CLI output, bundle-size baseline, and build-output hygiene all passed.
- Final `pnpm fmt && pnpm lint` — exit `0`; lint completed after declaration preparation with existing warnings only. Launch Workspace script formatter hunks produced by `pnpm fmt` were reverted as out-of-scope before staging.

Review: Thermo-nuclear review found the packet narrow enough for Phase 0. The added assertions are semantic and compact, the remaining source-text checks are secondary, broad Launch Workspace app typing is recorded instead of hidden, packed imports are represented without claiming the P0.1c performance matrix, and known TypeScript variance limits are explicitly left to their owning family packets.
Authority changes: P0.3 is now done. P0.1c is the next ready packet and must add packed/performance fixtures and measurements before P0.6 can synthesize Phase 0.
Still open: P0.1c owns packed/performance baselines. P0.6 remains blocked on P0.1c. BUG-38 remains open for P1A.0, and BUG-18T/BUG-18M/BUG-18S remain open for their family packets.
