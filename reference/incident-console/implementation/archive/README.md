# Archived implementation history

These files preserve superseded proposals and decision records for provenance only. They are not contract
authority, implementation instructions, or Beads inputs.

Current authority remains:

1. `../contracts/`, which contains the fully integrated contract baseline.
2. `../revision-spec/` and `revision-spec/`, which preserve revision provenance only; no archived revision
   overrides the contract baseline.
3. `../contracts/IMPLEMENTATION_WORKFLOW.md` for the implementation, review, test, and proof workflow.

The archived proposal, ADR, sufficiency-audit, research-snapshot, and fully integrated revision-chapter
files were transferred into the active contracts or superseded by accepted decisions before being moved here.
`proposals/MIGRATION_RECONCILIATION.md` records the disposition and the remaining pre-Beads work.
