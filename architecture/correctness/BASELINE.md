# P0.1a immutable public and behavioral baseline

This is the observational baseline for P0.1a. It records the repository at
commit `ff0f29b8dc03d451a92cecd807a075ca65212c84`; no tracked production
source, package manifest, test, or task status was changed by this packet. The
ignored library output regenerated by the required build and the authorized
process-only Packet output are classified below.

## Base, tree, and environment

- Base commit: `ff0f29b8dc03d451a92cecd807a075ca65212c84`.
- Base tree: clean (`git status --porcelain=v1 -uno` produced no rows). There
  was no pre-existing tracked user or planning work to classify.
- Pre-existing generated state: ignored `packages/flow-state/dist/` existed but
  was not fingerprinted. The first package test consumed that stale state, and
  the subsequent build destroyed it. Its 43-failure result is therefore an
  environment-dependent historical observation that cannot be reproduced from
  the base SHA alone.
- Packet output: this baseline plus process-only authority edits to `TASK.md`,
  `tasks/templates/PACKET.md`, `tasks/receipts/README.md`, and
  `tasks/PHASE_0.md`. These five Packet files define and apply two-commit
  closeout without changing runtime/public behavior. The receipt and status
  transition do not yet exist. Ignored `dist` is the current 46-file observed
  state: 45 files from remediation build B plus
  `dist/tsconfig.tsbuildinfo` written by the source typecheck.
- OS: `Darwin Arpits-MacBook-Pro.local 24.6.0 Darwin Kernel Version 24.6.0:
Wed Nov 5 21:30:44 PST 2025 root:xnu-11417.140.69.705.2~1/RELEASE_ARM64_T6041
arm64`.
- Node: `v22.22.1` (root requires `>=22.18.0`).
- pnpm: `10.13.1`; root `packageManager` is `pnpm@10.13.1`.
- TypeScript: `Version 6.0.3`.
- Lockfile: tracked `pnpm-lock.yaml`, `lockfileVersion: '9.0'`; no
  `package-lock.json` or `yarn.lock`; no lockfile mutation occurred.
- Install state: root and `packages/flow-state/node_modules` exist, while
  `examples/launch-workspace/node_modules` is absent. The Launch Workspace
  red gates below are consequently dependency-resolution failures, not a
  source-level result.

## Exact command evidence

### Initial literal sequence

These commands were run literally without a preparatory build. The first test
therefore observed unknown stale ignored output rather than a state derivable
from the base commit.

1. `pnpm --filter flow-state test` — exit `1`; 79 files and 509 tests, with 466
   passing and 43 failing in 5 files. No tracked file changed. Because the
   consumed `dist` was destroyed before it was fingerprinted, this is historical
   evidence only.
2. `pnpm --filter flow-state check:cli-source-types` — exit `0`; no tracked file
   changed. The controlled proof below establishes that this command writes or
   updates ignored `dist/tsconfig.tsbuildinfo`; the initial run's exact generated
   delta was not captured.
3. `pnpm --filter flow-state build` — exit `0`; no tracked file changed. It
   cleaned 53 stale output entries, emitted 34 bundle files, and left 45 ignored
   files after CLI preparation. The gate reported raw `213,967` bytes and gzip
   `41,368` bytes against baselines `223,466` and `43,517`, with maximum growth
   `1.05`.
4. `pnpm --filter @flow-state/launch-workspace test -- --run` — exit `1`; no
   tracked or persisted generated file changed. Ten suites failed at import
   time, three passed, and ten tests passed because
   `examples/launch-workspace/node_modules` is absent.
5. `pnpm --filter @flow-state/launch-workspace check:typescript-mode-proofs` —
   exit `2`; no tracked or persisted generated file changed. It reported missing
   `react`, `next`, `flow-state`, `@effect/vitest`, and Node types, followed by
   cascading implicit-`any` diagnostics.
6. `pnpm --filter @flow-state/launch-workspace build` — exit `2`; no tracked or
   persisted generated file changed. Its required TypeScript proof failed before
   `next build` ran.
7. `pnpm fmt && pnpm lint` — exit `1`; formatting completed, then lint reported
   the Launch Workspace resolution cascade. The formatter touched three tracked
   files outside packet scope; those provably formatter-generated edits were
   restored.

### Controlled generated-state observations

The review sequence normalized the package-test environment, but later
verification showed that the build's declaration output is not byte-deterministic
in this checkout. Every hash below identifies one observed run; none is a
reproducibility guarantee.

| Observation            | 45-file manifest SHA-256                                           | Result                                                    |
| ---------------------- | ------------------------------------------------------------------ | --------------------------------------------------------- |
| First controlled build | `744919ad3a3adfd009dd5acb84de1268d29f05b3bcb15c2e3ff581894015b8f0` | Exit `0`; emitted 34 files, passed size and hygiene gates |
| Final verifier build   | `b49b8f6d77ce7bb909a858e4662cf6e39de1835ac49ee6929e3d74825f320e65` | Exit `0`; emitted 34 files, passed the same gates         |
| Remediation build A    | `744919ad3a3adfd009dd5acb84de1268d29f05b3bcb15c2e3ff581894015b8f0` | Exit `0`; emitted 34 files, passed the same gates         |
| Remediation build B    | `744919ad3a3adfd009dd5acb84de1268d29f05b3bcb15c2e3ff581894015b8f0` | Exit `0`; emitted 34 files, passed the same gates         |

All four builds left 45 files and passed the size and hygiene gates. The first
and remediation builds reported raw `213,967` bytes and gzip `41,368` bytes
against baselines `223,466` and `43,517`, with maximum growth `1.05`; the
verifier manifest contained the same runtime bytes. The final verifier and
remediation manifests have 41 common paths: 36 kept the same hash, while the
five public entry declarations (`index`, `react-entry`, `testing`, `server`, and
`inspect`) changed hash. Four declaration-chunk paths also changed:

| Final verifier variant                    | First/remediation variant                 |
| ----------------------------------------- | ----------------------------------------- |
| `contract-runtime-E_dPnjck.d.mts` and map | `contract-runtime-CliQIyg8.d.mts` and map |
| `types-Ch9KH0n4.d.mts` and map            | `types-BgYfJYr0.d.mts` and map            |

All 21 runtime `.mjs` files were byte-identical across the two variants. The
five package-manifest entry targets remained present, and their runtime value
exports remained the matrix recorded below; sorted runtime export-name counts
were 23 root, 4 React, 10 testing, 1 server, and 34 inspect. The source entry
type inventory was unchanged at the same HEAD, but emitted declaration bytes
were not stable. The observations establish two declaration variants followed
by two identical remediation builds; they do not establish alternation, global
stability, or a causal mechanism.

After the first controlled build, the following gates were observed:

1. `pnpm --filter flow-state check:cli-source-types` — exit `0`. It preserved
   that run's 45 build-file hashes and added only
   `packages/flow-state/dist/tsconfig.tsbuildinfo`, size `197,142` bytes, SHA-256
   `30b1f675c253579281e5d37408d9f9c41feac355a885ec38b95d6f49db5e8ce0`.
   That observed 46-line manifest hashed to
   `e0f6bb6606de56f8d04d72d024517ba4824c65b42a1eace35ad74f3162f2df1c`.
2. `pnpm --filter flow-state test` — exit `1`; 79 files and 509 tests, with 470
   passing and 39 failing in the same 5 files. The 46-file manifest was unchanged
   during that test run.
3. Supplemental exact-ID command
   `pnpm exec vitest run packages/flow-state/src --reporter=json --outputFile=/tmp/p01a-post-build-vitest.json`
   — exit `1`. It repeated 470 passing and 39 failing tests and supplied the
   exact failed IDs listed below; it did not substitute for the literal package
   test.
4. `pnpm fmt && pnpm lint` — exit `1`. Formatting completed across 396 files,
   then lint repeated the Launch Workspace dependency-resolution cascade. The
   formatter's three out-of-scope tracked edits were restored.

After remediation build B, the verification was repeated against its observed
`744919...` variant:

1. `pnpm --filter flow-state check:cli-source-types` — exit `0`. It again
   preserved the 45 build hashes and added only `dist/tsconfig.tsbuildinfo` with
   the same size and hash recorded above, producing the observed 46-line
   aggregate `e0f6bb6606de56f8d04d72d024517ba4824c65b42a1eace35ad74f3162f2df1c`.
2. `pnpm --filter flow-state test` — exit `1`; 79 files and 509 tests, with 470
   passing and 39 failing in the same 5 files. The 46-file manifest was unchanged
   during the run.
3. `pnpm fmt && pnpm lint` — exit `1`. Formatting completed across 396 files,
   then lint repeated the known Launch Workspace dependency-resolution cascade.
   The three out-of-scope formatter edits were restored. The final 46-file
   manifest was unchanged from the remediation typecheck observation.

### Repeated normalized package-test failures

The 39 normalized failures are exact and grouped by file:

- `api-reference-generation-architecture.test.ts`:
  `api reference generation architecture > keeps the generated api reference artifact current with live exports`.
- `behavior-guidance-architecture.test.ts`:
  `behavior guidance architecture > keeps a standalone Goal 8 /goal prompt entry for autonomous behavior-system passes`.
- `behavior-scaffold-architecture.test.ts`:
  `behavior scaffold architecture > keeps scaffolds future, opt-in, and non-canonical in the narrowing contract`.
- `cli-test/behavior-cli.test.ts`:
  `behavior CLI script > renders live behavior coverage through the shared gateway loader`;
  `behavior CLI script > renders live behavior coverage as a stable JSON envelope through the compatibility CLI`.
- `cli-test/flow-state-cli.test.ts` (every ID below has the suite prefix
  `flow-state CLI script >`):
  `builds a behavior contract and renders the same contract in json mode`;
  `uses the working-directory behavior-contract.json path as the default package CLI contract location`;
  `renders live behavior coverage through the main flow-state CLI`;
  `renders live behavior coverage through the main flow-state CLI in json mode`;
  `renders the default behavior brief JSON shape as the raw behavior contract`;
  `diffs two behavior contract files in json mode through the main flow-state CLI`;
  `lists declared stories from a behavior gateway in text mode`;
  `resolves an explicit relative gateway path against the selected project root`;
  `filters story listings and emits a stable JSON envelope`;
  `emits a stable story describe JSON descriptor envelope`;
  `describes one story in text mode without running it`;
  `runs a declared story and emits compact execution facts by default`;
  `renders pending-work diagnostics for human debugging when requested`;
  `adds expectation-check deltas over the same run outcome in json mode`;
  `adds machine-readable pending-work diagnostics to the story-run JSON envelope`;
  `emits a stable story path list JSON envelope`;
  `lists shortest legal paths for a machine from repeated event candidates`;
  `checks an exact event sequence from an overridden start state in json mode`;
  `fails closed when exact-sequence checking is requested without any events`;
  `walks the full public workflow from declared facts to runtime evidence`;
  `saves a trace artifact from story run and summarizes it through the trace CLI`;
  `emits a stable trace summary JSON envelope for story-run traces`;
  `contextualizes a saved trace through the shared gateway loader in text mode`;
  `emits a stable contextualized trace summary JSON envelope`;
  `fails closed when contextualized summary cannot resolve a machine from the gateway`;
  `fails closed when trace summarize receives codebase context flags without --contextualize`;
  `renders an actor-focused proof slice from a saved trace artifact`;
  `reports an unknown proof actor through the typed CLI failure channel`;
  `emits issue-focused proof JSON even when no issues were recorded`;
  `fails closed when trace proof receives zero or multiple selectors`;
  `diffs two saved traces in text mode and reports changed sections`;
  `emits a stable trace diff JSON envelope`;
  `filters trace diff output to one named section`;
  `fails with a helpful message when a story id is missing`.

The initial 43-failure run additionally failed these four exact local-proof
tests, all of which pass after normalization:

- `flow-state CLI script > normalizes local proof JSON for trace summarize in json mode`.
- `flow-state CLI script > emits a stable correlation-focused proof JSON envelope from a local proof bundle`.
- `flow-state CLI script > renders a correlation-focused proof slice in text mode without an undefined headline`.
- `flow-state CLI script > renders the local proof inspection timeline through the timeline selector`.

That difference proves the initial stale `dist` affected those four outcomes;
it does not turn the historical 43-failure run into a base-commit result. One
historical run in unknown generated state cannot establish or exclude flakes.
Two literal normalized runs and the supplemental normalized reporter run agree
on all 39 failures. Each test run preserved its input manifest, but all three
used the observed `744919...` declaration variant, so this does not establish
the outcome under the verifier's `b49b8f...` variant or exclude flakes under
other runs or environments.

The normalized CLI failures remain dominated by
`Cannot find package 'flow-state' imported from .../.flow-state-cli-*/behavior-gateway.mjs`.
The three architecture failures remain the stale generated API reference and
two missing documentation source entries. The Launch Workspace commands fail
before behavior execution because its package-local `node_modules` is absent.
No source or tooling fix was attempted; BUG-21 is P0.1b and is a strict
non-goal here.

## Public export matrix

The package manifest publishes one package with five public entrypoints. The
value sets below are the exact sets asserted by
`packages/flow-state/src/public-api-types.test.ts`; type names are copied from
the five entry source files. Internal source modules and deep imports are not
part of this matrix.

### `flow-state`

Manifest target: `dist/index.mjs` / `dist/index.d.mts`.

Values:

- `createKey`
- `createTag`
- `after`
- `app`
- `can`
- `child`
- `ensure`
- `invalidate`
- `machine`
- `module`
- `observe`
- `orchestrators`
- `outcomes`
- `patch`
- `refresh`
- `resource`
- `run`
- `runtime`
- `selectView`
- `store`
- `stream`
- `transaction`
- `view`

Types:

- `RuntimeReadyLayer`
- `HostSignals`
- `InspectionLog`
- `NotificationScheduler`
- `OrchestratorSystem`
- `ResourceStore`
- `TraceLog`
- `FlowActionDefinition`
- `FlowActor`
- `FlowActorSnapshotTree`
- `FlowActorStartOptions`
- `FlowAppDefinition`
- `FlowAppLayerConfig`
- `FlowChildSnapshot`
- `FlowChildDefinition`
- `FlowConcurrencyPolicy`
- `FlowEnsureDefinition`
- `FlowEvent`
- `FlowEventTransitions`
- `FlowEventForState`
- `FlowInvalidationTarget`
- `FlowInvalidateDefinition`
- `FlowInvokeDescriptor`
- `FlowIssue`
- `FlowIssueSummary`
- `FlowKey`
- `FlowMachine`
- `FlowModuleInventory`
- `FlowOrchestratorDescriptor`
- `FlowMachineConfig`
- `FlowModuleDefinition`
- `FlowObserveDefinition`
- `FlowPatchDefinition`
- `FlowPreviewPatch`
- `FlowRefreshDefinition`
- `FlowResourceDefinition`
- `FlowResourceHydrationEntry`
- `FlowResourceRef`
- `FlowResourceSnapshot`
- `FlowReceipt`
- `FlowReceiptFacts`
- `FlowRehydratedTestHarness`
- `FlowRunDefinition`
- `FlowRuntime`
- `FlowRuntimeOrchestrators`
- `FlowRuntimeResources`
- `FlowSeededResource`
- `FlowSnapshot`
- `FlowStoreDescriptor`
- `FlowStreamDefinition`
- `FlowStreamSnapshot`
- `FlowTag`
- `FlowTimerSnapshot`
- `FlowTimerStatus`
- `FlowTransactionDefinition`
- `FlowTransactionConfig`
- `FlowTransactionSnapshot`
- `FlowTestChildSummary`
- `FlowTestChildTree`
- `FlowTestChildTreeNode`
- `FlowTransitionArgs`
- `FlowViewConfig`
- `FlowViewDefinition`
- `FlowViewSource`
- `InferMachineContext`
- `InferMachineEvent`
- `InferMachineState`
- `SelectionSource`

### `flow-state/react`

Manifest target: `dist/react-entry.mjs` / `dist/react-entry.d.mts`.

Values:

- `FlowProvider`
- `use` (alias of `useFlowActor`)
- `useResource` (alias of `useFlowResource`)
- `useView` (alias of `useFlowView`)

Types:

- `FlowProviderProps`

### `flow-state/testing`

Manifest target: `dist/testing.mjs` / `dist/testing.d.mts`.

Values:

- `createControlledStream`
- `formatHarnessTracePretty`
- `formatPendingWorkPretty`
- `formatScenarioTranscript`
- `formatTransactionEventsPretty`
- `runFlowStory`
- `runFlowStoryWithDiagnostics`
- `storyToTest`
- `test`
- `flowTest`

Types:

- `FlowAppFixtureName`
- `FlowIssueSummary`
- `FlowModelDescriptor`
- `FlowModelReplayConfig`
- `FlowModelPath`
- `FlowModelTraversalOptions`
- `FlowRehydratedTestHarness`
- `FlowStartedTestBuilder`
- `FlowStoryRunBlocked`
- `FlowStoryRunBlockedReason`
- `FlowStoryRunOutcome`
- `FlowStoryRunResult`
- `FlowStoryTestCheck`
- `FlowStoryTestCheckKind`
- `FlowStoryTestReport`
- `FlowTestBuilder`
- `FlowTestCache`
- `FlowTestChildSummary`
- `FlowTestChildTree`
- `FlowTestChildTreeNode`
- `FlowTestHarness`
- `FlowTestPendingChild`
- `FlowTestPendingMailbox`
- `FlowTestPendingTimer`
- `FlowTestPendingWork`
- `FlowTestTimers`
- `FlowTestTransactions`
- `FlowTestProgressBounds`
- `FlowModelStep`
- `FlowTestApi`
- `FlowTestAppBuilder`
- `FlowTestModelConfig`
- `FlowTestRehydrationConfig`
- `FlowTestScenarioBuilder`
- `FlowTestWithConfig`

### `flow-state/server`

Manifest target: `dist/server.mjs` / `dist/server.d.mts`.

Values:

- `withRequestRuntime`

Types:

- `FlowRuntimeBootActorSnapshot`
- `FlowRuntimeBootOptions`
- `FlowRuntimeBootPayload`
- `FlowRuntimeHydratedBoot`

### `flow-state/inspect`

Manifest target: `dist/inspect.mjs` / `dist/inspect.d.mts`.

Values:

- `analyzeTrace`
- `attachInspectionSink`
- `captureTrace`
- `compressTraceArtifact`
- `createLocalInspectionProof`
- `createInspectionBufferSink`
- `decompressTraceArtifact`
- `diffTrace`
- `exportTraceArtifact`
- `formatInspectionEvent`
- `formatInspectionEventPretty`
- `formatInspectionTimeline`
- `formatInspectionTimelinePretty`
- `formatNoTransitionSummary`
- `formatRehydrationSummary`
- `formatResourceFreshnessReport`
- `formatTrace`
- `formatTracePretty`
- `formatTransactionOverlapSummary`
- `flowStories`
- `graphOf`
- `importTraceArtifact`
- `inspectActions`
- `inspectMicrosteps`
- `inspectTransition`
- `storyToDoc`
- `summarizeTrace`
- `whyNoTransition`
- `buildBehaviorContract`
- `sliceBehaviorContract`
- `diffBehaviorContracts`
- `renderBehaviorDiff`
- `renderBehaviorContract`
- `renderBehaviorCoverage`

Types:

- `FlowGraphChildSpec`
- `FlowGraphDescriptor`
- `FlowGraphEdge`
- `FlowGraphEventlessTransition`
- `FlowGraphJson`
- `FlowGraphJsonEdge`
- `FlowGraphJsonOptions`
- `FlowGraphJsonNode`
- `FlowGraphNode`
- `FlowGraphOwnershipOverlay`
- `FlowGraphOwnershipSource`
- `FlowGraphPath`
- `FlowGraphPathFromEventsOptions`
- `FlowGraphStep`
- `FlowGraphTimedTransition`
- `FlowGraphTraversalOptions`
- `FlowAppliedMicrostepGuardResult`
- `FlowActionFact`
- `FlowActionInspection`
- `FlowActionInspectionFact`
- `FlowActionInspectionPhase`
- `FlowInspectionEvent`
- `FlowInspectionBufferSink`
- `FlowInspectionExportOptions`
- `FlowInspectionFilter`
- `FlowLocalInspectionProof`
- `FlowInspectionObserver`
- `FlowInspectionSink`
- `FlowInspectionSinkConnector`
- `FlowInspectionSinkOptions`
- `FlowInspectionSinkTarget`
- `FlowInspectionRetentionPolicy`
- `FlowInspectionSnapshot`
- `FlowInspectionSnapshotEvent`
- `FlowInspectionSubscription`
- `FlowMicrostepInspection`
- `FlowMicrostepInspectionLimitReached`
- `FlowMicrostepInspectionStep`
- `FlowMicrostepTrigger`
- `FlowNoTransitionExplanation`
- `FlowNoTransitionReason`
- `FlowPlannedEffectFact`
- `FlowPlannedEffectOperation`
- `FlowUpdateInspectionFact`
- `FlowTraceAnalysisDescriptor`
- `FlowTraceArtifact`
- `FlowTraceArtifactOptions`
- `FlowTraceArtifactSnapshot`
- `FlowTraceArtifactVersion`
- `FlowTraceDiffDescriptor`
- `FlowTraceDiffSection`
- `FlowTraceDiffSectionName`
- `FlowTraceDiffSummary`
- `FlowTraceStateChange`
- `FlowTraceIncidentBucketCounts`
- `FlowTraceIncidentOutcomeCounts`
- `FlowTraceIncidentStep`
- `FlowTraceIncidentSummary`
- `FlowRuntimeInspection`
- `FlowStory`
- `FlowStoryCoverageDescriptor`
- `FlowStoryCoverageReason`
- `FlowStoryCoverageStatus`
- `FlowStoryCoverageStory`
- `FlowStoryCoverageSummary`
- `FlowStoryDocDescriptor`
- `FlowStoryDocEvent`
- `FlowStoryDocExpectation`
- `FlowStoryDocSeed`
- `FlowStoryDocStart`
- `FlowStoryExpectedFacts`
- `FlowStorySeed`
- `FlowStorySetup`
- `FlowStoryStart`
- `FlowStoriesDescriptor`
- `FlowTransitionActionCounts`
- `FlowTransitionCandidate`
- `FlowTransitionCandidateGuardResult`
- `FlowTransitionInspection`
- `FlowTraceDescriptor`
- `FlowTraceActorNode`
- `FlowTraceChildDetail`
- `FlowTraceChildOutcome`
- `FlowTraceChildRetryCause`
- `FlowTraceChildSpawnReason`
- `FlowTraceChildStopReason`
- `FlowTraceCorrelationDetails`
- `FlowTraceOutcome`
- `FlowTraceOutcomeKind`
- `FlowTraceOutcomeSource`
- `FlowTraceResourceDetail`
- `FlowTraceResourceFetchOutcome`
- `FlowTraceResourceFreshnessChange`
- `FlowTraceResourceFreshnessReason`
- `FlowTraceResourceInvalidationReason`
- `FlowTraceResourceQueryMode`
- `FlowTraceStreamCompletion`
- `FlowTraceStreamDetail`
- `FlowTraceStreamInterruptReason`
- `FlowTraceTimerDetail`
- `FlowTraceTimerInterruptReason`
- `FlowTraceTimerOutcome`
- `FlowTraceTransactionDetail`
- `FlowTraceTransactionAttemptTiming`
- `FlowTraceTransactionOverlapCause`
- `FlowTraceTransactionPreviewSummary`
- `FlowTraceTransactionQueueCause`
- `FlowTraceTransactionRollbackSummary`
- `FlowTraceTransactionRoutedEvent`
- `FlowTraceReport`
- `FlowBehaviorApp`
- `FlowBehaviorBuildTarget`
- `FlowBehaviorContract`
- `FlowBehaviorGateway`
- `FlowBehaviorMachine`
- `FlowBehaviorModule`
- `FlowBehaviorResource`
- `FlowBehaviorState`
- `FlowBehaviorStory`
- `FlowBehaviorStream`
- `FlowBehaviorTransaction`
- `FlowBehaviorTransition`
- `FlowBehaviorView`
- `FlowBehaviorRenderOptions`
- `FlowBehaviorCoverageRenderOptions`
- `FlowBehaviorAppSummary`
- `FlowBehaviorCoverageObligation`
- `FlowBehaviorCoverageObligationDiff`
- `FlowBehaviorCoverageObligationKind`
- `FlowBehaviorDiffDescriptor`
- `FlowBehaviorDiffItemChange`
- `FlowBehaviorDiffOptions`
- `FlowBehaviorDiffSection`
- `FlowBehaviorDiffSectionName`
- `FlowBehaviorMachineChange`

The package's `./package.json` metadata export and CLI bin are package
metadata, not TypeScript entry values.

## Caller and compatibility inventory

`rg` over source, examples, docs, and typecheck fixtures found consumers of all
five public paths. Launch Workspace uses root, React, testing, server, and
inspect imports from its app assembly, feature modules, tests, and client shell;
the package typecheck fixtures exercise the same five declaration paths. No
Launch Workspace/client consumer imports `packages/flow-state/src/...` as a
public path; the docs generator's source reads are build tooling, not client
imports. The
entry ownership assertions in `public-typing-architecture.test.ts` and
`public-api-types.test.ts` reject cross-entry builder leakage and assert the
negative legacy names (`flow`, `mutation`, `createRuntime`, and
`createControlledEffect`) remain unpublished.

The manifest and source agree on a single package with these subpaths. The
generated docs API artifact is not fully current: its `flow-state/testing`
section omits `runFlowStoryWithDiagnostics`, which explains the dedicated
API-reference architecture failure and remains an open documentation-sync
issue for a later packet.

## Scope and open concerns

- P0.1a changed no runtime behavior, public source, package manifests, tests, or
  task status. Its current Packet output is this baseline and the authorized
  process-only authority alignment above; the required build regenerated
  ignored `dist` output. No receipt or status transition exists yet.
- Effect A/E/R, layer construction, ownership, lifecycle, and resource scoping
  are N/A because this packet introduces no Effect program or runtime object.
  No tests were added; compatibility is observed through the unchanged source,
  manifest targets, runtime artifacts, and public export inventory.
- There are no P0.1a dependency receipts and no linked DEC/BUG/BT/TI/CV rows.
  BUG-21 tooling/build-resolution repair belongs to P0.1b and was not attempted.
- Declaration-build byte instability remains unresolved and has no proved cause:
  the same HEAD produced two observed declaration variants while runtime output,
  size, hygiene, entry targets, and source inventories stayed fixed. P0.1a only
  records that evidence and does not assign or implement a tooling fix.
- The exact red states above are the starting evidence for P0.1b/P0.2/P0.4/P0.5.
  Launch Workspace verification must be rerun after its missing install/link
  state is made available; the package test's CLI and generated-doc failures
  require their owning packets, not a baseline-only workaround.

## P0.1c packed and performance fixture baseline

P0.1c extends the immutable baseline with repeatable packed-consumer and
performance fixtures. These measurements were produced on 2026-07-12 with
Node `v22.22.1` on `darwin/arm64` by
`pnpm --filter flow-state p0-1c:measure`, which first runs
`pnpm --filter flow-state build`, then emits declarations for the packed type
fixtures, runs Launch Workspace declaration proof, and measures fixed
small/medium/adversarial scaling tiers with three repetitions per tier.

### Packed fixture directories and commands

The packed type fixtures are ordinary workspace consumers that import through
package entry points instead of source paths:

| Fixture                                                  | Public entries covered                         | Command                                                                                                         |
| -------------------------------------------------------- | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `examples/typescript-proof-multi-entry`                  | root, React 19, testing, server, and inspect   | `pnpm exec tsc --pretty false --extendedDiagnostics -p examples/typescript-proof-multi-entry/tsconfig.json`     |
| `examples/typescript-proof-packed-react-18`              | React entry under React 18 consumer types      | `pnpm exec tsc --pretty false --extendedDiagnostics -p examples/typescript-proof-packed-react-18/tsconfig.json` |
| `examples/typescript-proof-packed-react-19`              | React entry under React 19 consumer types      | `pnpm exec tsc --pretty false --extendedDiagnostics -p examples/typescript-proof-packed-react-19/tsconfig.json` |
| `packages/flow-state/typecheck`                          | private/deep-import negatives and shared types | `pnpm --filter flow-state check:typescript-mode-proofs`                                                         |
| `examples/launch-workspace`                              | Launch Workspace declaration behavior          | `pnpm --filter @flow-state/launch-workspace check:typescript-mode-proofs`                                       |
| `packages/flow-state/scripts/measure-p0-1c-baseline.mjs` | type, package, and scaling measurements        | `pnpm --filter flow-state p0-1c:measure`                                                                        |

The React 18 fixture pins `react@18.3.1`, `react-dom@18.3.1`,
`@types/react@18.3.31`, and `@types/react-dom@18.3.7`, and its tsconfig maps
non-relative `react` type imports to the fixture's own type package. That
matters in this workspace because the `flow-state` symlink also has React 19
dev types installed; the mapping keeps the fixture representative of a packed
consumer resolving peer-owned React types.

### Type, declaration, and package measurements

The P0.1c measurement script reported these TypeScript extended diagnostics and
declaration byte counts. Times are milliseconds from TypeScript's own
diagnostics, while command duration is the outer wall-clock measurement from the
script.

| Fixture                                         | Files | Types | Instantiations | Check ms | Emit ms | Total ms | Command ms | Declaration bytes | Declaration gzip bytes |
| ----------------------------------------------- | ----: | ----: | -------------: | -------: | ------: | -------: | ---------: | ----------------: | ---------------------: |
| multi-entry root/react19/testing/server/inspect |   237 | 7,602 |         59,814 |      120 |     370 |      860 |      1,148 |         3,267,023 |                 88,009 |
| packed React 18                                 |   234 | 4,011 |         13,881 |       60 |      10 |      420 |        716 |               244 |                    196 |
| packed React 19                                 |   233 | 4,744 |         14,453 |       70 |       0 |      440 |        727 |               244 |                    196 |

The built package output measured by the same run was:

| Output set                                         | Files |     Bytes | Gzip bytes |
| -------------------------------------------------- | ----: | --------: | ---------: |
| `packages/flow-state/dist` declarations            |     7 |   152,351 |     23,275 |
| all `packages/flow-state/dist` files               |    45 | 2,142,622 |    385,884 |
| publish payload proxy (`package.json` plus `dist`) |    46 | 2,144,809 |    386,249 |

Launch Workspace declaration behavior is now normalized: the command
`pnpm --filter @flow-state/launch-workspace check:typescript-mode-proofs`
exited `0` in `2,062ms` during the P0.1c measurement run.

### Fixed scaling tiers

P0.1c defines three repeatable operation-count tiers. The measurements are
baselines, not budgets; P0.6 selects capacities from these numbers and product
needs, and P5.4 compares ratios by rerunning the same command.

| Tier        | Operations | Key depth |
| ----------- | ---------: | --------: |
| small       |         64 |         3 |
| medium      |        256 |         6 |
| adversarial |      1,024 |        12 |

Each row below reports median milliseconds and range milliseconds across three
repetitions. Heap deltas are intentionally kept in the JSON output as an
allocation proxy, but not promoted into a gate because garbage collection can
make short-run deltas negative.

| Fixture category         |  Small median/range ms | Medium median/range ms | Adversarial median/range ms | Operation-count result recorded |
| ------------------------ | ---------------------: | ---------------------: | --------------------------: | ------------------------------- |
| canonical key depth      |    0.066 / 0.062-0.237 |    0.389 / 0.355-0.393 |         1.599 / 1.473-1.680 | encoded key bytes               |
| collection size          |    1.897 / 0.983-4.524 |    1.741 / 1.718-2.175 |      21.470 / 20.870-21.825 | dehydrated resource count       |
| subscriber churn         |    1.222 / 1.145-1.843 |    2.056 / 2.055-2.525 |         7.610 / 7.359-7.771 | subscribe/unsubscribe count     |
| nested patch waves       |    1.252 / 0.754-2.003 |    2.648 / 2.551-3.258 |      15.397 / 15.214-15.940 | patch count and final value     |
| actor mailbox contention |   6.480 / 5.719-10.560 | 16.691 / 16.301-17.037 |      87.252 / 86.718-89.416 | actor count and receipt count   |
| transaction pressure     | 12.628 / 12.002-17.775 | 61.449 / 60.263-62.847 |   654.647 / 557.173-780.047 | context count and receipt count |
| stream pressure          |    5.167 / 4.925-5.934 | 18.546 / 18.248-18.713 |   108.284 / 107.347-110.082 | emitted value and receipt count |
| evidence retention       |    4.083 / 3.870-4.447 | 16.509 / 16.463-17.051 |      93.619 / 93.260-95.107 | receipt count and JSON bytes    |
| restore                  |    4.689 / 4.367-4.754 | 16.667 / 16.618-16.922 |      97.010 / 95.219-98.315 | restored count and JSON bytes   |
