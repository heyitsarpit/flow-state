import behaviorContract from "../../generated/behavior-contract.json";
import { BehaviorBriefSections } from "../../components/behavior-brief-sections";

# Behavior Contract

This page is the shared application behavior brief rendered from the canonical
generated contract artifact.

It is intentionally terse and shared between humans and AI agents. Treat it as
the shortest app-level onboarding and planning surface for the current behavior
contract loop: `flow-state behavior build`, `flow-state behavior render`, and
`flow-state behavior diff`.

When you need the proof app or current runtime honesty surface, continue with
[Launch Workspace](/guide/launch-workspace) and
[Current Status](/reference/status) instead of treating this page as a second
prose guide.

<BehaviorBriefSections contract={behaviorContract} />
