# Flow State

Flow State is an experimental Effect-native runtime for teams building
mid-to-large applications with cached resources, optimistic transactions,
workflows, streams, deterministic tests, and inspection under one scoped owner.

It is an opinionated alternative to assembling TanStack Query and XState for the
same application boundary. It is not a drop-in replacement and does not claim
feature parity with either library.

## Alpha surface

| Need                    | Flow State owner                                                               |
| ----------------------- | ------------------------------------------------------------------------------ |
| Canonical shared data   | `flow.resource`, keyed refs, freshness, and invalidation                       |
| Server writes           | `flow.transaction` with optimistic preview, rollback, and typed outcomes       |
| Workflows               | `flow.machine`, state-owned resources, streams, timers, and children           |
| Reusable UI projections | `flow.view`                                                                    |
| React                   | Caller-owned `FlowProvider` plus `useActor`, `useResource`, and `useView`      |
| Deterministic proof     | `test(...)`, `test.app(...)`, virtual time, receipts, issues, and pending work |
| Inspection              | Behavior contracts, stories, graphs, traces, and the package CLI               |

The package exports `flow-state`, `flow-state/react`, `flow-state/testing`,
`flow-state/server`, and `flow-state/inspect`. The five small applications are
focused recipes; [Incident Console](/examples#incident-console) is the real
client/server proof over HTTP and SSE.

## Start here

Follow [Getting Started](/getting-started) for the single supported onboarding
path. Read [Current Status](/reference/status) before adopting the alpha, because
the machine model, server integration, compatibility range, and non-goals are
deliberately narrow.
