# Integration tests

Integration tests are repository-level contract proofs. Each test must name the
contract/proof IDs, production owner, setup, action, observable assertion, failure
condition, and why a package-local test is insufficient.
