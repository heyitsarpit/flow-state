# Repository tests

Package-owned deterministic tests live with the rewrite package. Cross-package
behavior tests live under `tests/integration/` and exercise public routes,
production runtime ownership, hosts, persistence, CLI, and packed artifacts.
